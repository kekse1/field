([Back to the **Index**](./README.md))

# Library
Some helper functions, etc.

## `isArrayType(_value)`
Checks whether the argument is any type of Array. So either a regular `Array`
or any kind of `TypedArray` (e.g. `Uint8Array`). Supports **only instances**!

