([Back to the **Index**](./README.md))

# Library
Some helper functions, etc.

## `isArrayType()`
Checks whether the argument is a type of Array. So either a regular `Array`
or any kind of `TypedArray`. Supports **only instances**!

